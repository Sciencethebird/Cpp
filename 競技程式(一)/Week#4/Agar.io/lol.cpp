#include<iostream>
#include<string.h>
#include<math.h>
#include<algorithm>
using namespace std;

int main()
{
    char  a[20000];
    char trans[15];

    //long long data[100000];

    //while(fgets(a, sizeof(a), stdin) != NULL)
    //{
        int j = 0;
        long long sz = 0;
        cin >> a;
        printf("%s", a);
        //cout << " lol " << strlen(a) << endl;
    //}
    return 0;
}
